services:
  relay:
    build: .
    image: relay-docker:local
    container_name: tunnel-relay
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      GUI_USER: admin
      GUI_PASS: change-me-now
      PORT: "8080"
      DATA_DIR: /data
    volumes:
      - ./data:/data
